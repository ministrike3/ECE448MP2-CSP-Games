from board import board
from board import move

class tree(object):
    def __init__(self):
        self.data=data
        self.children=[]

    def add_child(self, obj):
        self.children.append(obj)
